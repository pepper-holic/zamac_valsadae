# UI 검토 — 재현본 기반 지적사항 12건 (2026-08-09, 2026-08-10 12건 전체 적용 완료)

재현본(HTML 목업) 기준으로 작성된 UI 평가 12건을 실제 코드(`frontend/src/components/*.tsx`, `frontend/src/App.css`)와
대조해 검증한 결과입니다. 각 항목은 검증 상태와 근거 코드 위치, 필요 시 참고 사항을 담고 있습니다.
**2026-08-10에 12건 모두 수정을 적용했습니다** — 하단 "적용 내역" 참고.

## 검증 상태 요약

| # | 관점 | 지적 요약 | 상태 |
|---|---|---|---|
| 1 | 일관성 | "새 프로젝트" 버튼이 고스트 스타일이라 눈에 덜 띔 | 적용 완료 (2026-08-10) |
| 2 | 접근성 | Undo/Redo가 아이콘만 표시, 발견성이 hover 툴팁 의존 | 적용 완료 (2026-08-10) |
| 3 | 정보구조 | 툴바 3구역에 컨트롤이 몰려 flex-wrap으로 줄바꿈 | 적용 완료 (2026-08-10) |
| 4 | 시각 디자인 | 도구 레일 라벨 10px, 버튼 높이 44px 미만 | 적용 완료 (2026-08-10, 라벨만) |
| 5 | 정보구조 | 도구 패널 헤더가 왼쪽 레일 라벨과 중복 표시 | 적용 완료 (2026-08-10) |
| 6 | 인터랙션 | 260px 고정폭 컨트롤 행이 flex-wrap으로 흔들림 | 적용 완료 (2026-08-10) |
| 7 | 시각 디자인 | 자막 스타일 색상 선택이 브라우저 기본 `input[type=color]` | 적용 완료 (2026-08-10) |
| 8 | 인터랙션 | 타임라인 리사이즈 핸들 6px, 최소 마커 폭 3px | 적용 완료 (2026-08-10) |
| 9 | 일관성 | 카라오케 강조색(#ffd54a) 하드코딩 | 적용 완료 (2026-08-10) |
| 10 | 정보구조 | 세그먼트 행 체크박스/편집/검토완료가 opacity:0으로 숨음 | 적용 완료 (2026-08-10) |
| 11 | 일관성 | 필터 탭 상태 구분이 색상에만 의존 | 적용 완료 (2026-08-10) |
| 12 | 일관성 | 상세 검수 패널 액션 버튼 6개가 동일 스타일, 삭제 버튼도 같은 무게 | 적용 완료 (2026-08-10) |

## 항목별 근거

### #1 "새 프로젝트" 버튼 발견성
`toolbar-ghost-button`(투명 배경, 12.5px)과 `toolbar-export-button`(pill, accent 배경 + 그림자)이 같은 헤더에 공존.
`Toolbar.tsx:173-190`, `App.css:136-152`, `App.css:168-190`.
※ 다만 "새 프로젝트"는 브랜드 zone에, "내보내기"는 `toolbar-zone-spacer`로 분리된 반대쪽 끝에 있어 물리적으로 인접하지는 않음 — 지적의 핵심(스타일 약함)은 유효.

### #2 Undo/Redo 발견성
아이콘(↶/↷)만 노출, `aria-label="되돌리기"` / `aria-label="다시 실행"`은 존재해 스크린리더 접근성은 확보됨
(`Toolbar.tsx:270-289`). 문제는 **접근성**이 아니라 **시각적 발견성**(sighted user가 아이콘 의미를 못 알아챔)에 가까움 —
"일관성/발견성" 카테고리로 재분류 권장.

### #3 툴바 과밀
브랜드/워크스페이스/파일 3개 zone에 select 2개 + ghost 버튼 3개 + undo/redo/도움말/내보내기까지 한 헤더에 배치,
`.toolbar { flex-wrap: wrap }` (`App.css:68`)로만 대응.

### #4 도구 레일 라벨/버튼 크기
`tool-rail-label { font-size: 10px }` (`App.css:669`)는 확인되어 라벨이 작다는 지적은 타당.
단 버튼 높이는 `padding 8px+7px + glyph 20px + gap 4px + label ~12px ≈ 51px`로 계산되어 44px 터치 타겟 기준을
오히려 상회함 (`App.css:627-671`). 실측(브라우저 렌더링) 검증 필요 — 코드 기준으로는 "높이 부족" 주장은 반증됨.

### #5 도구 패널 헤더 중복
`App.tsx:650-651`의 `tool-panel-column-head`가 `<h3>{TOOL_TITLES[activeTool]}</h3>`로 "전사"/"번역" 등을 표시하는데
왼쪽 `ToolRail`의 `tool-rail-label`도 같은 텍스트 사용.

### #6 260px 고정폭 컨트롤 줄바꿈
`.tool-panel-column { width: 260px }` (`App.css:674`)인데 `SubtitleStylePanel.tsx`의 `panel-row`는
select+number+checkbox+button을 한 줄에 배치하고 `.panel-row { flex-wrap: wrap }` (`App.css:1650`)로만 처리.

### #7 색상 선택기 기본값
`SubtitleStylePanel.tsx:138-170`에 `<input type="color">`가 그대로 3곳(글자색/외곽선색/배경색) 사용,
`App.css:1701-1709`는 테두리/크기만 커스텀.

### #8 타임라인 핸들/마커 크기
`.timeline-marker-handle { width: 6px }` (`App.css:932`), `.timeline-marker { min-width: 3px }` (`App.css:885`).

### #9 카라오케 색상 하드코딩
`.subtitle-karaoke-highlight { color: #ffd54a; }` (`App.css:813`) — `--accent`/`--text` 등 디자인 토큰 미사용.

### #10 세그먼트 행 컨트롤 숨김
`segment-select-checkbox` (`App.css:1261`), `segment-row-actions` (`App.css:1234`), `reviewed-toggle` (`App.css:1217`)
모두 기본 `opacity: 0`, `:hover`/`.selected`/`.checked`에서만 노출.

### #11 필터 탭 색상 전용 구분
`SegmentList.tsx`의 필터 탭은 `active`/`warn`/`ok` 클래스 조합뿐이고 CSS(`App.css:1404-1418`)도 배경색만 변경 —
아이콘·패턴 등 추가 신호 없음.

### #12 상세 검수 패널 액션 버튼 무게
`SegmentDetailPanel.tsx:196-226`의 `.timing-actions` 4개 버튼 중 삭제 버튼만 `danger-button` 클래스가 있지만
CSS(`App.css:1607-1611`)는 `color`/`border-color`만 빨간색으로 바꿀 뿐 padding·크기·배경은 동일. `margin-left: auto`로
오른쪽에 붙지만 시각적 무게는 나머지 버튼과 같음.

## 권장 우선순위 (2026-08-10 적용 완료)

1. [x] 도구 패널 헤더 ↔ 레일 라벨 중복 제거 (#5) — 패널 헤더를 도구명 반복 대신 한 줄 설명(`TOOL_DESCRIPTIONS`)으로 교체. `App.tsx`, `App.css`
2. [x] 도구 레일 라벨 폰트 확대 (#4의 라벨 부분만) — `tool-rail-label` 10px → 11.5px. `App.css`
3. [x] 위험 액션(삭제) 시각적 분리 강화 (#12) — `.timing-actions .danger-button`에 배경색·굵은 글씨·hover 반전 추가. `App.css`
4. [x] 세그먼트 행 컨트롤 상시 노출 또는 최소 투명도 부여 (#10) — 체크박스/행 액션/검토완료 토글 기본 `opacity: 0` → `0.35`. `App.css`
5. [x] 카라오케 색상 등 하드코딩 값의 디자인 토큰화 (#9) — `--karaoke-highlight` CSS 변수 추가(라이트/다크), `.subtitle-karaoke-highlight`에서 사용. `index.css`, `App.css`

실제 프로젝트 데이터로 dev 서버(vite + uvicorn)를 띄워 5개 항목 모두 DOM computed style로 검증 완료.

## 나머지 7건 적용 내역 (2026-08-10)

6. [x] #1 "새 프로젝트" 버튼 발견성 — `.toolbar-primary-ghost` 클래스 추가(테두리+배경, hover 시 accent 톤). `Toolbar.tsx`, `App.css`
7. [x] #2 Undo/Redo 발견성 — 개별 아이콘 버튼을 `.toolbar-history-group`(테두리+배경의 세그먼트형 클러스터)으로 묶어 "여기 컨트롤이 있다"는 신호를 hover 없이도 제공. `Toolbar.tsx`, `App.css`
8. [x] #3 툴바 과밀 — `.toolbar-zone`에 패딩/라운드를 부여하고, 900px 이하에서는 배경까지 입혀 줄바꿈 시에도 그룹이 시각적으로 분리되게 함. `App.css`
9. [x] #6 260px 컨트롤 줄바꿈 — `SubtitleStylePanel`의 각 label+control 쌍을 `.panel-field`로 묶어, 줄바꿈이 일어나도 라벨과 입력이 따로 떨어지지 않도록 함(필드 단위로만 줄바꿈). `SubtitleStylePanel.tsx`, `App.css`
10. [x] #7 색상 선택기 — `input[type=color]`의 네이티브 여백/테두리를 제거하고(`-webkit-color-swatch` 등) 둥근 스와치 칩 모양 + hover/focus 링을 추가(피커 자체는 브라우저 기본 유지, 트리거만 커스텀). `App.css`
11. [x] #8 타임라인 핸들/마커 — `timeline-marker-handle` 6px→9px, `timeline-marker` 최소폭 3px→5px. `App.css`
12. [x] #11 필터 탭 색상 의존 — warn/ok 탭에 아이콘(○/⚠/✓) 추가, active 상태에 굵은 글씨+inset 링을 더해 색맹 사용자도 상태를 구분 가능하게 함. `SegmentList.tsx`, `App.css`

추가로 `SegmentList` 컴포넌트 테스트(11개, 필터/페이지네이션/일괄작업/찾기바꾸기)를 신규 작성했습니다 — `SegmentList.test.tsx`.
전체 vitest 스위트(23개) 통과, `tsc --noEmit` 통과, 실제 프로젝트 데이터로 브라우저 렌더링까지 확인했습니다.
