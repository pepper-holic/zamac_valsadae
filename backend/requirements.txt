fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
pydantic>=2.9

# 데스크톱 창(WebView2 기반) - Chrome 브라우저 탭 대신 네이티브 프로그램 창으로 UI를 띄웁니다.
pywebview>=5.3
faster-whisper>=1.0.3
transformers>=4.45
sentencepiece>=0.2
sacremoses>=0.1
ctranslate2>=4.4

# 화자 분리(선택 기능) - HuggingFace 게이트 모델이라 사용 전 HF_TOKEN 설정과
# https://huggingface.co/pyannote/speaker-diarization-3.1 이용약관 동의가 필요합니다.
pyannote-audio>=3.3

# GPU 전사 가속(선택 기능, Windows) - NVIDIA GPU가 있고 CUDA Toolkit을 따로
# 설치하지 않은 환경에서, faster-whisper(ctranslate2)가 필요로 하는 cuBLAS/
# cuDNN DLL을 pip으로 제공합니다. 없어도 앱은 CPU로 정상 동작합니다
# (whisper_service._detect_device가 자동 감지해 CPU로 전사).
nvidia-cublas-cu12
nvidia-cudnn-cu12

pytest>=8.3
httpx>=0.27
